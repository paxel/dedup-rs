[package]
name = "dedup-gui"
version = "0.1.0"
edition = "2024"

[dependencies]
dedup-core = { path = "../dedup-core" }
eframe = "0.35"
egui = "0.35"
crossbeam-channel = "0.5"
chrono = "0.4"
rfd = "0.17"

[dev-dependencies]
egui_kittest = { version = "0.35", features = ["wgpu", "snapshot"] }
tempfile = "3.10"
