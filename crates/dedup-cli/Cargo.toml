[package]
name = "dedup-cli"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "dedup"
path = "src/main.rs"

[dependencies]
dedup-core = { path = "../dedup-core" }
dedup-gui = { path = "../dedup-gui" }
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
